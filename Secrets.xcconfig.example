// Checked-in base config for secret injection.
// Keep default empty in repository.
WEATHER_API_KEY =
// Optional: route requests via your backend proxy and keep provider key off-device.
// Example: WEATHER_PROXY_BASE_URL = https://weather-proxy.example.com
WEATHER_PROXY_BASE_URL =

// Optional local include allows each developer to keep credentials out of git.
#include? "Secrets.xcconfig"
